# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/

# Type checking and test caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE and operating-system files
.vscode/
.idea/
.DS_Store

# Environment files
.env
.envrc

# Raw corpus and generated data
data/raw/*
data/processed/*
data/output/*

# Local model files (root-level only; do not match src/models/)
/models/
*.safetensors
*.bin

# The moulinette grading binary, wherever it's unzipped to run it locally
# (project root, per the README's Moulinette instructions). Never ours to
# ship — same "downloadable from intra, don't commit it" rule as above.
/moulinette
/moulinette-ubuntu
/moulinette-fedora